const NoSessionDiv = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center text-2xl text-red-500">
      You must be logged in to access this page
    </div>
  );
};

export default NoSessionDiv;
